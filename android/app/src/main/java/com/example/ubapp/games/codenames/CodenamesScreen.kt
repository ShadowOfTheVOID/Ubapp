package com.example.ubapp.games.codenames

import androidx.compose.runtime.Composable
import com.example.ubapp.games.TODOScreen

@Composable
fun CodenamesScreen() = TODOScreen(
    title = "Codenames",
    note = "Engine ported (CodenamesEngine.kt). UI + full 400-word bank pending.",
)
