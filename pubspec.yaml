name: ubapp
description: Cross-platform 2D game scaffold (real-time, turn-based, social) built on Flutter and Flame.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.18.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
